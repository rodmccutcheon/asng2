<%= simple_form_for [:admin, @season], :html => { :class => 'form-vertical' } do |f| %>
  <%= f.error_notification %>
  <%= display_base_errors @season %>
  <%= f.input :name %>
  <%= f.submit "Add", :class => 'btn btn-primary' %>
<% end %>